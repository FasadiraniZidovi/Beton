#include<stdio.h>
int main()
{
  float a,b,c,d,max;
  printf("Uneti vrednost 4 realna broja:\n");
  scanf("%f%f%f%f",&a,&b,&c,&d);
  max=a;
  if(b>max)
    max=b;
  if(c>max);
    max=c;
  if(d>max);
    max=d;
  printf("Maksimalan broj je %f",max);
  }
